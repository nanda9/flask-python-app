alertmanager:
  enabled: true

  config:
    global:
      smtp_smarthost: "smtp.gmail.com:587"
      smtp_from: "nandakumar.jraju@gmail.com"
      smtp_auth_username: "nandakumar.jraju@gmail.com"
      smtp_auth_password: "dzmfczlfthmmbgio"
      smtp_require_tls: true

    route:
      receiver: gmail
      group_by:
        - namespace
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 12h
      routes:
        - matchers:
            - alertname = "Watchdog"
          receiver: "null"

    receivers:
      - name: gmail
        email_configs:
          - to: "nandakumar.jraju@gmail.com"
            send_resolved: true

      - name: "null"