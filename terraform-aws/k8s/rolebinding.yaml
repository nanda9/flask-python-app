apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: watchtower-pod-reader
  namespace: default

subjects:
  - kind: ServiceAccount
    name: watchtower-sa
    namespace: default

roleRef:
  kind: Role
  name: watchtower-pod-reader
  apiGroup: rbac.authorization.k8s.io