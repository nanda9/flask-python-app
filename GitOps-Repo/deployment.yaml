apiVersion: apps/v1
kind: Deployment

metadata:
  name: flask-deployment
  namespace: dev

spec:
  replicas: {{ .Values.replicaCount }}

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  selector:
    matchLabels:
      app: flask

  template:
    metadata:
      labels:
        app: flask
    spec:
      serviceAccountName: flask-sa
      
      containers:
      - name: flask

<<<<<<< HEAD
        image:
          {{ .Values.image.repository }}:{{ .Values.image.tag }}
=======
        image: nandadocker23/my-python-app:4412e4dd7f4019a1b3b666868ef622f052babee4
        imagePullPolicy: IfNotPresent
>>>>>>> 8ce351f1870ef671a2f401a2b4d4c9204076272c

        ports:
        - containerPort: 5000

        envFrom:
        - configMapRef:
            name: flask-config
        - secretRef:
            name: flask-secret

        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 20

        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"